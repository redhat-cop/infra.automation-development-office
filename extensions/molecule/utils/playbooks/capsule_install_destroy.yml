---
- name: Destroy integration_capsule_install scenario
  hosts: localhost
  gather_facts: false
  become: false

  tasks:
    - name: Destroy step placeholder
      ansible.builtin.debug:
        msg: integration_capsule_install destroy complete
