---
- name: Validate source behavior with local fixtures
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Run executable regression checks
      ansible.builtin.command:
        argv:
          - python3
          - "{{ playbook_dir }}/check_runner.py"
      changed_when: false
