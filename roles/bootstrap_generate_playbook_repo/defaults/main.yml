---
bootstrap_generate_playbook_repo_dest: "{{ playbook_dir }}"
bootstrap_generate_playbook_repo_seed_src: >-
  {{ role_path }}/files/playbook_repo_seed
bootstrap_generate_playbook_repo_force: false

bootstrap_generate_playbook_repo_git_mode: manual
bootstrap_generate_playbook_repo_git_remote: origin
bootstrap_generate_playbook_repo_git_branch: main
bootstrap_generate_playbook_repo_git_message: "Generate bootstrap playbook repo"
bootstrap_generate_playbook_repo_git_username: oauth2
bootstrap_generate_playbook_repo_git_token: ""
# Set to 'bearer' for Bitbucket (uses Authorization: Bearer header instead of credential store).
bootstrap_generate_playbook_repo_git_auth_mode: "basic"
bootstrap_generate_playbook_repo_git_sync_before_push: true
bootstrap_generate_playbook_repo_pause_after_generate: false

# Controller project sync installs collections from Hub via requirements.yml only.
# Collection trees are never committed into the git playbook repo.
bootstrap_generate_playbook_repo_write_galaxy_requirements: true
bootstrap_generate_playbook_repo_galaxy_ignore_certs: false

# Leave empty so generated collections/requirements.yml has no version pin. Set only when you must pin.
bootstrap_generate_playbook_repo_infra_ado_collection_version: ""

bootstrap_generate_playbook_repo_component: all

bootstrap_generate_playbook_repo_component_map:
  all:
    - all

  openshift:
    - aap
    - acs
    - acm
    - cert_manager
    - console
    - devspaces
    - dev_hub
    - dirsrv
    - eck
    - gitops
    - gitlab
    - grafana
    - bookstack
    - kafka
    - minio
    - oadp
    - ocp_compliance
    - ocp_descheduler
    - ocp_virtualization
    - openshift
    - quay
    - rhbk
    - gitlab_runner
    - web_terminal
    - zabbix

  rhel:
    - rhel
    - satellite_server_install
    - satellite_client_tools
    - satellite_content_view
    - satellite_capsule_install
    - satellite_oidc
    - idm_server_install
    - idm_replica_install
    - idm_client_tools
    - idm_dns_install
    - aap
    - dirsrv
    - eck
    - gitlab
    - grafana
    - kafka
    - rhbk
    - compliance
    - stig
    - rhel_system_roles

  patching:
    - rhel
    - satellite_server_install
    - satellite_client_tools
    - satellite_content_view
    - satellite_capsule_install
    - satellite_oidc
    - idm_server_install
    - idm_replica_install
    - idm_client_tools
    - idm_dns_install

  provision:
    - openshift_virt

  openshift_virt:
    - openshift_virt

  aap:
    - aap

  rhbk:
    - rhbk
    - rhbk_standalone

  rhbk_standalone:
    - rhbk_standalone

  grafana:
    - grafana
    - grafana_standalone

  grafana_standalone:
    - grafana_standalone

  bookstack:
    - bookstack

  minio:
    - minio

  netbox:
    - netbox

  kafka:
    - kafka

  gitlab:
    - gitlab
    - gitlab_standalone

  gitlab_standalone:
    - gitlab_standalone

  satellite:
    - satellite_server_install
    - satellite_client_tools
    - satellite_content_view
    - satellite_capsule_install

  idm:
    - idm_server_install
    - idm_replica_install
    - idm_client_tools
    - idm_dns_install

  satellite_server_install:
    - satellite_server_install

  satellite_client_tools:
    - satellite_client_tools

  satellite_content_view:
    - satellite_content_view

  satellite_capsule_install:
    - satellite_capsule_install

  satellite_oidc:
    - satellite_oidc

  idm_server_install:
    - idm_server_install

  idm_replica_install:
    - idm_replica_install

  idm_client_tools:
    - idm_client_tools

  idm_dns_install:
    - idm_dns_install

  rhel-patching:
    - rhel

  stig:
    - stig

  rhel-stig:
    - stig

  compliance:
    - compliance

  oadp:
    - oadp

  ocp_compliance:
    - ocp_compliance

  ocp_descheduler:
    - ocp_descheduler

  ocp_virtualization:
    - ocp_virtualization

  gitlab_runner:
    - gitlab_runner

  web_terminal:
    - web_terminal

  elastic:
    - eck

  ec2_ami_copy:
    - ec2_ami_copy

  # AWS platform umbrella. Add future apps (ec2_instance, etc.) here.
  aws:
    - ec2_ami_copy

bootstrap_generate_playbook_repo_generated_playbooks:

  - src: aap/ado-aap-ocp-install-bootstrap.yml
    dest: playbooks/aap/ado-aap-ocp-install-bootstrap.yml
    target_platform: openshift
    category: platform
    app: aap

  - src: aap/ado-aap-rhel-install-bootstrap.yml
    dest: playbooks/aap/ado-aap-rhel-install-bootstrap.yml
    target_platform: rhel
    category: platform
    app: aap

  - src: acs/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/acs/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: platform
    app: acs

  - src: acs/ado-acs-report.yml
    dest: playbooks/acs/ado-acs-report.yml
    target_platform: openshift
    category: platform
    app: acs

  - src: aws/ado-copy-ami-bootstrap.yml
    dest: playbooks/aws/ado-copy-ami-bootstrap.yml
    target_platform: aws
    category: aws
    app: ec2_ami_copy

  - src: acm/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/acm/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: platform
    app: acm

  - src: cert-manager/ado-configure-idm-acme-clusterissuer.yml
    dest: playbooks/cert-manager/ado-configure-idm-acme-clusterissuer.yml
    target_platform: openshift
    category: platform
    app: cert_manager

  - src: cert-manager/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/cert-manager/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: platform
    app: cert_manager

  - src: cert-manager/ado-install-and-configure-awspca-bootstrap.yml
    dest: playbooks/cert-manager/ado-install-and-configure-awspca-bootstrap.yml
    target_platform: openshift
    category: platform
    app: cert_manager

  - src: cert-manager/ado-update-default-ingress-cert-bootstrap.yml
    dest: playbooks/cert-manager/ado-update-default-ingress-cert-bootstrap.yml
    target_platform: openshift
    category: platform
    app: cert_manager

  - src: openshift/ado-configure-console-banner-bootstrap.yml
    dest: playbooks/openshift/ado-configure-console-banner-bootstrap.yml
    target_platform: openshift
    category: platform
    app: console

  - src: devspaces/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/devspaces/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: devspaces

  - src: dirsrv/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/dirsrv/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: dirsrv

  - src: dirsrv/ado-ldap-modify-bootstrap.yml
    dest: playbooks/dirsrv/ado-ldap-modify-bootstrap.yml
    target_platform: openshift
    category: applications
    app: dirsrv

  - src: dirsrv/ado-search-users-groups-bootstrap.yml
    dest: playbooks/dirsrv/ado-search-users-groups-bootstrap.yml
    target_platform: openshift
    category: applications
    app: dirsrv

  - src: eck/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/eck/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: eck

  - src: gitlab/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/gitlab/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: gitlab

  - src: gitlab/ado-install-gitlab-standalone-bootstrap.yml
    dest: playbooks/gitlab/ado-install-gitlab-standalone-bootstrap.yml
    target_platform: linux
    category: applications
    app: gitlab_standalone

  - src: gitops/ado-deploy-bootstrap.yml
    dest: playbooks/gitops/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: gitops

  - src: grafana/ado-create-custom-route.yml
    dest: playbooks/grafana/ado-create-custom-route.yml
    target_platform: openshift
    category: platform
    app: grafana_alternate_route

  - src: grafana/ado-deploy-install-bootstrap.yml
    dest: playbooks/grafana/ado-deploy-install-bootstrap.yml
    target_platform: openshift
    category: platform
    app: grafana

  - src: grafana/ado-deploy-datasource-bootstrap.yml
    dest: playbooks/grafana/ado-deploy-datasource-bootstrap.yml
    target_platform: openshift
    category: platform
    app: grafana_datasources

  - src: grafana/ado-deploy-folders-bootstrap.yml
    dest: playbooks/grafana/ado-deploy-folders-bootstrap.yml
    target_platform: openshift
    category: platform
    app: grafana_folders

  - src: grafana/ado-deploy-bootstrap.yml
    dest: playbooks/grafana/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: grafana

  - src: grafana/ado-deploy-dashboards-bootstrap.yml
    dest: playbooks/grafana/ado-deploy-dashboards-bootstrap.yml
    target_platform: openshift
    category: platform
    app: grafana_dashboards

  - src: grafana/ado-deploy-alerts-bootstrap.yml
    dest: playbooks/grafana/ado-deploy-alerts-bootstrap.yml
    target_platform: openshift
    category: platform
    app: grafana_alerts

  - src: grafana/ado-deploy-oidc-bootstrap.yml
    dest: playbooks/grafana/ado-deploy-oidc-bootstrap.yml
    target_platform: openshift
    category: platform
    app: grafana_oidc

  - src: grafana/ado-deploy-email-bootstrap.yml
    dest: playbooks/grafana/ado-deploy-email-bootstrap.yml
    target_platform: openshift
    category: platform
    app: grafana_email

  - src: grafana/ado-install-grafana-standalone-bootstrap.yml
    dest: playbooks/grafana/ado-install-grafana-standalone-bootstrap.yml
    target_platform: linux
    category: platform
    app: grafana_standalone

  - src: docs/ado-deploy-install-bootstrap.yml
    dest: playbooks/docs/ado-deploy-install-bootstrap.yml
    target_platform: openshift
    category: applications
    app: bookstack

  - src: docs/ado-deploy-oidc-bootstrap.yml
    dest: playbooks/docs/ado-deploy-oidc-bootstrap.yml
    target_platform: openshift
    category: applications
    app: bookstack_oidc

  - src: netbox/ado-configure-netbox-oidc-bootstrap.yml
    dest: playbooks/netbox/ado-configure-netbox-oidc-bootstrap.yml
    target_platform: openshift
    category: applications
    app: netbox_oidc

  - src: idm/ado-install-replica-bootstrap.yml
    dest: playbooks/idm/ado-install-replica-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_replica_install

  - src: idm/ado-install-server-bootstrap.yml
    dest: playbooks/idm/ado-install-server-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_server_install

  - src: idm/ado-manage-client-bootstrap.yml
    dest: playbooks/idm/ado-manage-client-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_client_tools

  - src: idm/ado-manage-dns-bootstrap.yml
    dest: playbooks/idm/ado-manage-dns-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_dns_install

  - src: idm/ado-manage-server-settings-bootstrap.yml
    dest: playbooks/idm/ado-manage-server-settings-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_server_install

  - src: idm/ado-manage-sudo-bootstrap.yml
    dest: playbooks/idm/ado-manage-sudo-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_server_install

  - src: idm/ado-manage-ad-trust-bootstrap.yml
    dest: playbooks/idm/ado-manage-ad-trust-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_ad_trust_install

  - src: idm/ado-manage-topology-bootstrap.yml
    dest: playbooks/idm/ado-manage-topology-bootstrap.yml
    target_platform: linux
    category: patching
    app: idm_server_install

  - src: jira/ado-create-stories-bootstrap.yml
    dest: playbooks/jira/ado-create-stories-bootstrap.yml
    target_platform: all
    category: utilities
    app: jira

  - src: kafka/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/kafka/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: applications
    app: kafka

  - src: kafka/ado-setup-alerting-bootstrap.yml
    dest: playbooks/kafka/ado-setup-alerting-bootstrap.yml
    target_platform: openshift
    category: applications
    app: kafka

  - src: oadp/ado-deploy-bootstrap.yml
    dest: playbooks/oadp/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: oadp

  - src: ocp_compliance/ado-deploy-bootstrap.yml
    dest: playbooks/ocp_compliance/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: ocp_compliance

  - src: web_terminal/ado-deploy-bootstrap.yml
    dest: playbooks/web_terminal/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: web_terminal

  - src: gitlab_runner/ado-deploy-bootstrap.yml
    dest: playbooks/gitlab_runner/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: gitlab_runner

  - src: ocp_virtualization/ado-deploy-bootstrap.yml
    dest: playbooks/ocp_virtualization/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: ocp_virtualization

  - src: ocp_descheduler/ado-deploy-bootstrap.yml
    dest: playbooks/ocp_descheduler/ado-deploy-bootstrap.yml
    target_platform: openshift
    category: platform
    app: ocp_descheduler

  - src: openshift/ado-configure-ldap-auth-bootstrap.yml
    dest: playbooks/openshift/ado-configure-ldap-auth-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_ldap_auth

  - src: openshift/ado-configure-oauth-with-rhbk-bootstrap.yml
    dest: playbooks/openshift/ado-configure-oauth-with-rhbk-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_oauth_rhbk

  - src: openshift/ado-create-htpass-user-bootstrap.yml
    dest: playbooks/openshift/ado-create-htpass-user-bootstrap.yml
    target_platform: openshift
    category: platform
    app: admin_htpasswd

  - src: vpn/ado-manage-vpn-access-bootstrap.yml
    dest: playbooks/vpn/ado-manage-vpn-access-bootstrap.yml
    target_platform: localhost
    category: platform
    app: vpn_access

  - src: openshift/ado-install-nfs-csi-bootstrap.yml
    dest: playbooks/openshift/ado-install-nfs-csi-bootstrap.yml
    target_platform: openshift
    category: platform
    app: nfs_csi

  - src: openshift/ado-install-iscsi-csi-bootstrap.yml
    dest: playbooks/openshift/ado-install-iscsi-csi-bootstrap.yml
    target_platform: openshift
    category: platform
    app: iscsi_csi

  - src: openshift/ado-discover-routes-print-bootstrap.yml
    dest: playbooks/openshift/ado-discover-routes-print-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_discover_routes_print

  - src: openshift/ado-print-alt-routes-bootstrap.yml
    dest: playbooks/openshift/ado-print-alt-routes-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_print_alt_routes

  - src: openshift/ado-add-alt-routes-bootstrap.yml
    dest: playbooks/openshift/ado-add-alt-routes-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_add_alt_routes

  - src: openshift/ado-add-ingress-with-route-bootstrap.yml
    dest: playbooks/openshift/ado-add-ingress-with-route-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_add_ingress_with_route

  - src: openshift/ado-update-pull-secret-bootstrap.yml
    dest: playbooks/openshift/ado-update-pull-secret-bootstrap.yml
    target_platform: openshift
    category: platform
    app: openshift_update_pull_secret

  - src: provision/ado-provision-openshift-virt-vm-bootstrap.yml
    dest: playbooks/provision/ado-provision-openshift-virt-vm-bootstrap.yml
    target_platform: openshift
    category: provision
    app: openshift_virt

  - src: quay/ado-deploy-install-bootstrap.yml
    dest: playbooks/quay/ado-deploy-install-bootstrap.yml
    target_platform: openshift
    category: applications
    app: quay

  - src: quay/ado-deploy-oidc-bootstrap.yml
    dest: playbooks/quay/ado-deploy-oidc-bootstrap.yml
    target_platform: openshift
    category: applications
    app: quay_oidc

  - src: minio/ado-deploy-install-bootstrap.yml
    dest: playbooks/minio/ado-deploy-install-bootstrap.yml
    target_platform: openshift
    category: applications
    app: minio

  - src: minio/ado-deploy-oidc-bootstrap.yml
    dest: playbooks/minio/ado-deploy-oidc-bootstrap.yml
    target_platform: openshift
    category: applications
    app: minio_oidc

  - src: dev_hub/ado-deploy-install-bootstrap.yml
    dest: playbooks/dev_hub/ado-deploy-install-bootstrap.yml
    target_platform: openshift
    category: applications
    app: dev_hub

  - src: dev_hub/ado-deploy-oidc-bootstrap.yml
    dest: playbooks/dev_hub/ado-deploy-oidc-bootstrap.yml
    target_platform: openshift
    category: applications
    app: dev_hub_oidc

  - src: zabbix/ado-deploy-install-bootstrap.yml
    dest: playbooks/zabbix/ado-deploy-install-bootstrap.yml
    target_platform: openshift
    category: applications
    app: zabbix

  - src: zabbix/ado-configure-saml-bootstrap.yml
    dest: playbooks/zabbix/ado-configure-saml-bootstrap.yml
    target_platform: openshift
    category: applications
    app: zabbix_saml

  - src: rhbk/ado-deploy-and-configure-bootstrap.yml
    dest: playbooks/rhbk/ado-deploy-and-configure-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk

  - src: rhbk/ado-install-rhbk-standalone-bootstrap.yml
    dest: playbooks/rhbk/ado-install-rhbk-standalone-bootstrap.yml
    target_platform: linux
    category: platform
    app: rhbk_standalone

  - src: rhbk/ado-manage-client-bootstrap.yml
    dest: playbooks/rhbk/ado-manage-client-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk_client

  # Shared by realm/client/mapper/scopes/IdP playbooks (import_tasks). Must be
  # seeded or Contoller fails with "Unable to retrieve file contents".
  - src: rhbk/ado-rhbk-api-pre_tasks.yml
    dest: playbooks/rhbk/ado-rhbk-api-pre_tasks.yml
    target_platform: openshift
    category: platform
    app: rhbk

  - src: rhbk/ado-manage-entraid-idp.yml
    dest: playbooks/rhbk/ado-manage-entraid-idp.yml
    target_platform: openshift
    category: platform
    app: rhbk_idp

  - src: rhbk/ado-manage-realm-bootstrap.yml
    dest: playbooks/rhbk/ado-manage-realm-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk_realm

  - src: rhbk/ado-enable-realm-bootstrap.yml
    dest: playbooks/rhbk/ado-enable-realm-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk_realm

  - src: rhbk/ado-manage-client-scope-bootstrap.yml
    dest: playbooks/rhbk/ado-manage-client-scope-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk_client

  - src: rhbk/ado-realm-bootstrap.yml
    dest: playbooks/rhbk/ado-realm-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk_federation

  - src: rhbk/ado-setup-mapper-bootstrap.yml
    dest: playbooks/rhbk/ado-setup-mapper-bootstrap.yml
    target_platform: openshift
    category: platform
    app: rhbk_mapper

  - src: rhel/ado-patch-host-bootstrap.yml
    dest: playbooks/rhel/ado-patch-host-bootstrap.yml
    target_platform: linux
    category: patching
    app: rhel

  - src: rhel/ado-compliance-bootstrap.yml
    dest: playbooks/rhel/ado-compliance-bootstrap.yml
    target_platform: linux
    category: compliance
    app: compliance

  - src: rhel/ado-stig-hardening-bootstrap.yml
    dest: playbooks/rhel/ado-stig-hardening-bootstrap.yml
    target_platform: linux
    category: compliance
    app: stig

  - src: satellite/ado-install-satellite-bootstrap.yml
    dest: playbooks/satellite/ado-install-satellite-bootstrap.yml
    target_platform: linux
    category: satellite
    app: satellite_server_install

  - src: satellite/ado-configure-satellite-bootstrap.yml
    dest: playbooks/satellite/ado-configure-satellite-bootstrap.yml
    target_platform: linux
    category: satellite
    app: satellite_server_install

  - src: satellite/ado-manage-content-view-bootstrap.yml
    dest: playbooks/satellite/ado-manage-content-view-bootstrap.yml
    target_platform: linux
    category: satellite
    app: satellite_content_view

  - src: satellite/ado-register-to-satellite-bootstrap.yml
    dest: playbooks/satellite/ado-register-to-satellite-bootstrap.yml
    target_platform: linux
    category: patching
    app: satellite_client_tools

  - src: satellite/ado-configure-satellite-oidc-bootstrap.yml
    dest: playbooks/satellite/ado-configure-satellite-oidc-bootstrap.yml
    target_platform: linux
    category: satellite
    app: satellite_oidc
