bugfixes:
  - ocp_htpasswd_admin - Hash all htpasswd users via a single stdin Python job
    that emits the secret body and username list, avoiding Jinja ``| keys`` and
    looped ``set_fact`` drops that left only the first user (e.g. ``admin``)
    in the OpenShift secret.
