---
bugfixes:
  - bootstrap_generate_env_vars - RHBK OpenShift TLS supports ``manual`` mode
    (``tls.crt`` / ``tls.key`` into vault) alongside edge and cert-manager; stop
    silently defaulting Keycloak admin password to ``redhat123`` when unset.
  - bootstrap_generate_env_vars - Admin HTPasswd rejects empty passwords instead of
    writing blank hashes (no silent ``redhat123`` default).
minor_changes:
  - ado-preflight-ui - Move Credentials and Git under AAP Configuration tabs;
    RHBK OpenShift form adds TLS mode (edge / cert-manager / own cert upload).
