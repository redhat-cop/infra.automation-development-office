---
bugfixes:
  - >-
    grafana_upload_dashboards - Always sanitize and truncate Grafana dashboard
    UIDs to 40 characters (schema-v2 ``metadata.name``). Long work-item names
    such as ``rhacs-vulnerability-dashboard.json__Security`` no longer cause
    ``uid too long, max 40 characters``. Also strip classic ``id`` from v2
    upload specs.
