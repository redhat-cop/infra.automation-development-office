bugfixes:
  - ocp_htpasswd_admin - Revert Contoller EE package install / ``htpasswd
    -nbB`` hashing. Restore Ansible ``password_hash('bcrypt')`` which the
    supported EE already provides; Contoller pods cannot install
    ``httpd-tools``.
