---
bugfixes:
  - >-
    bootstrap_generate_env_vars - Admin HTPasswd vault templates no longer
    hardcode ``redhat123``. Write real ``htpasswd_users`` into
    ``vault_htpass_admin.yml``, ``vault_admin_htpasswd.yml``, and
    ``vault_openshift.yml`` from preflight; drop the bogus
    ``admin_htpasswd: redhat123`` stub key.
  - >-
    bootstrap_generate_playbook_repo - Create Admin HTPasswd playbook loads
    dedicated htpasswd vaults last so Contoller uses the preflight password.
