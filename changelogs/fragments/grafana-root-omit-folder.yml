---
bugfixes:
  - >-
    grafana_upload_dashboards - For Grafana root boards, omit ``folderUid`` /
    ``folderId`` entirely (empty uid and ``folderId: 0`` both returned
    ``folder not found`` on current Grafana).
