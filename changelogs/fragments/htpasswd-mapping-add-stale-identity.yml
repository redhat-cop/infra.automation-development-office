bugfixes:
  - ocp_htpasswd_admin - Default ``mappingMethod`` to ``add`` and refresh the
    existing HTPasswd IdP in place. Stale Identity ``htpasswd:admin`` with IdP
    ``htpasswd-admin`` caused console ``Could not create user`` under
    ``claim``. Also delete leftover ``htpasswd:<user>`` Identities when the
    IdP name is ``htpasswd-admin``.
