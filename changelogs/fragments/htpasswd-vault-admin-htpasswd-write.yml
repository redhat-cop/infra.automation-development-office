bugfixes:
  - bootstrap_generate_env_vars - Write Admin HTPasswd ``htpasswd_users`` /
    ``htpasswd_pass`` into both ``vault_htpass_admin.yml`` (Contoller JT
    component) and ``vault_admin_htpasswd.yml`` (env-gen stub name). Fail hard
    when Admin HTPasswd is selected with an empty password — no silent default.
