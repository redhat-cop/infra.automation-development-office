doc_changes:
  - bootstrap_controller - Clarify Admin HTPasswd JT survey labels so
    ``htpasswd_action`` (add/replace/remove users) is distinct from
    ``state`` (present keeps the Login button, absent deletes the IdP).
