---
bugfixes:
  - >-
    ocp_htpasswd_admin - Hash all HTPasswd users in one step so secondary
    users (for example ``chaddie``) are written into the secret, not only
    the first username.
  - >-
    ado-preflight-ui - Typing Additional ansible-playbook options no longer
    swaps the playbook preview pane back to the bootstrap scaffolding
    command; click Preview commands to refresh.
