---
bugfixes:
  - >-
    ocp_htpasswd_admin - ``state=absent`` now removes the IdP named in
    ``htpasswd_idp_name`` (and matching ``htpasswd_secret``), not the
    hardcoded ``htpasswd-admin`` fallback.
