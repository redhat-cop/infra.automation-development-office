bugfixes:
  - ocp_htpasswd_admin - On ``state=absent``, remove only the configured Login
    button IdP; refuse when that name is missing but other HTPasswd IdPs exist
    (no more silent delete of default ``htpasswd-admin``).
  - bootstrap_generate_playbook_repo - Local component runs pass form
    ``htpasswd_idp_name`` / ``htpasswd_secret`` as extra vars so delete/add
    track the preflight Login button name even when group_vars are stale.
